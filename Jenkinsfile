pipeline {
     agent any
     
     environment {
           name = "rajan"
            city = "nagpur"
              }
              
              stages {
                   stage('build') {
                       steps {
                            sh ' echo "$name" '
                            sh ' echo "$city" '
                       }
                   }
                    
              }
}
